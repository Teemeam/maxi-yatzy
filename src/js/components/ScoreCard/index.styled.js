import styled from '@emotion/styled';

export const Container = styled.div`
  position: relative;
  max-width: 500px;
  width: 94%;
  margin: 0 auto;
  .tbody-wrapper {
    position: relative;
    display: flex;
    flex-direction: row;
    width: 100%;
    padding: 0 0 50px 0;
  }
`;