import styled from '@emotion/styled';

export const Container = styled.div`
  position: relative;
  max-width: 800px;
  margin: 0 auto;
  .tbody-wrapper {
    display: flex;
    flex-direction: row;
  }
`;