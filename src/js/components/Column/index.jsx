import React from 'react';

/* Styles */
import * as s from './index.styled.js';

const Column = (props) => {
  return (<s.Container>
    <div>Sarake</div>
  </s.Container>);
};

export default Column;