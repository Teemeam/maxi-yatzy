import React from 'react';
import styled from '@emotion/styled';

/* Components */

/* Styles */
const AppContainer = styled.div`
  /* */
`;

const App = (props) => {
  return (
    <AppContainer>
      <div>Toimii!</div>
    </AppContainer>
  );
};

export default App;
